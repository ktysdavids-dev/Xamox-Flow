# Creado automáticamente si no existía backend/.env (JUGAR_AHORA / JUGAR_MOVIL).
# Para Atlas u otro servidor, edita MONGO_URL aquí.

MONGO_URL=mongodb://127.0.0.1:27017
DB_NAME=xamox_flow
JWT_SECRET=cambia-esto-en-produccion-xamox-local
CORS_ORIGINS=*
BACKEND_PUBLIC_URL=http://localhost:8000
